#!/bin/bash
cd ~/work/device/xiaomi/fleur/prebuilts
aria2c https://github.com/RT1648/Extras/releases/download/13.0.9.0/vendor.img
ls
cd ~/work
rm -rf system/bpf
git clone https://github.com/PixelExperience/system_bpf.git system/bpf
rm -rf hal_fingerprint_default.te && rm -rf hal_health_default.te && rm -rf  rild.te && rm -rf hal_light_default.te && rm -rf mediacodec.te
export CCACHE_DIR=/tmp/ccache
export CCACHE_EXEC=$(which ccache)
export USE_CCACHE=1
ccache -M 50G
ccache -o compression=true
ccache -z
. build/envsetup.sh
lunch "$LUNCH_COMBO"
make $TARGET -j$(nproc --all)

